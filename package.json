{
  "name": "xpedia",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "type-check": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "build:extension": "node scripts/generate-extension-config.js && npx esbuild src/chrome-extension/content.ts --bundle --outfile=dist/chrome-extension/content.js --platform=browser --target=chrome120 && npx esbuild src/chrome-extension/background.ts --bundle --outfile=dist/chrome-extension/background.js --platform=browser --target=chrome120 --format=iife && npx esbuild src/chrome-extension/popup.ts --bundle --outfile=dist/chrome-extension/popup.js --platform=browser --target=chrome120 --format=iife && cp src/chrome-extension/manifest.json src/chrome-extension/popup.html dist/chrome-extension/"
  },
  "dependencies": {
    "@supabase/ssr": "^0.8.0",
    "@supabase/supabase-js": "^2.97.0",
    "next": "16.1.6",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "stripe": "^20.4.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/chrome": "^0.1.36",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitest/coverage-v8": "^4.0.18",
    "esbuild": "^0.27.3",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.1",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^4.0.18"
  }
}
